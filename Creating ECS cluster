switch (ASG_name) {
	case "dm-ec2-etl":
		return ["dm-ecs"]
		break
	case "dm2-DebtManager-activemq-asg":
		return ["dm2-cluster"]
		break
	case "test || test1 || test2":
		return ["123-cluster", "PRD-cluster", "dev-cluster", "tdf-cluster"]
		break
}
